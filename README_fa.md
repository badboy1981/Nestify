# Nestify

**Nestify** یک ابزار خط فرمان (CLI) قدرتمند و سبک نوشته‌شده با زبان Go است که به توسعه‌دهندگان کمک می‌کند ساختار پروژه‌های خود را به‌راحتی **اسکن**، **تحلیل** و **ایجاد** کنند.

مناسب برای انواع پروژه‌ها:  
بک‌اند (Go, .NET, Node.js)، فرانت‌اند (React, Vue, Angular)، اپلیکیشن موبایل (Flutter, MAUI)، بازی (Unity)، پروژه‌های Python و هر ساختار دیگری.

## ویژگی‌های کلیدی

- **اسکن هوشمند پروژه**  
  ساختار کامل پوشه‌ها و فایل‌ها را به‌صورت JSON یا Markdown تولید می‌کند.

- **حالت فقط پوشه‌ها** (`--folders-only`)  
  نمای کلی و تمیز از معماری پروژه بدون نمایش تک‌تک فایل‌ها.

- **پشتیبانی از فایل `.nestifyignore`**  
  دقیقاً مانند `.gitignore` عمل می‌کند. می‌توانید پوشه‌ها و فایل‌های ناخواسته (مثل `bin/`, `obj/`, `.vs/`, `node_modules/`, `TestResults/` و ...) را مخفی کنید تا خروجی کاملاً تمیز باشد.

- **تحلیل اسکلت پروژه** (`analyze`)  
  نقش هر پوشه را به‌صورت خودکار تشخیص می‌دهد (نقطه ورود، هسته اصلی کد، تست‌ها، منابع، تنظیمات و ...).

- **ایجاد ساختار از قالب** (`init`)  
  با یک فایل JSON ساده، ساختار کامل پروژه را در چند ثانیه بسازید.

- **نمایش درختی زیبا در ترمینال**  
  ساختار پروژه به‌صورت خوانا و با فرمت tree نمایش داده می‌شود.

## پیش‌نیازها

- Go 1.16 یا بالاتر
- پکیج خارجی: `github.com/xlab/treeprint`

## نصب

```bash
git clone https://github.com/badboy1981/Nestify.git
cd Nestify
go build -o nestify ./cmd/nestify

# اختیاری: اضافه کردن به PATH سیستم
sudo mv nestify /usr/local/bin/   # لینوکس/macOS
# یا در ویندوز به پوشه‌ای در PATH کپی کنید
```

حالا می‌توانید از هر جایی دستور `nestify` را اجرا کنید.

## دستورات اصلی

### 1. اسکن پروژه (`scan`)

```bash
nestify scan --path <مسیر-پروژه> [گزینه‌ها]
```

**گزینه‌های مهم:**

- `--tree` → نمایش درختی در ترمینال + ذخیره Markdown در `scan_output.md`
- `--folders-only` → فقط پوشه‌ها (ایده‌آل برای نمایش معماری کلی)
- خروجی JSON همیشه در `scan_output.json` ذخیره می‌شود

**مثال:**
```bash
nestify scan --path ./MyProject --tree --folders-only
```

### 2. تحلیل اسکلت پروژه (`analyze`)

```bash
nestify analyze --path <مسیر-پروژه>
```

گزارش نقش پوشه‌ها را در ترمینال نمایش می‌دهد و در فایل `skeleton_report.md` ذخیره می‌کند.

### 3. ایجاد پروژه جدید از قالب (`init`)

```bash
nestify init --template <فایل-قالب-json> --path <مسیر-جدید>
```

**مثال:**
```bash
nestify init --template templates/dotnet-maui.json --path ./MyNewApp
```

## فایل `.nestifyignore`

این فایل در ریشه پروژه‌ای که اسکن می‌کنید قرار می‌گیرد و به شما اجازه می‌دهد موارد غیرضروری را مخفی کنید.

**مثال برای پروژه .NET / MAUI:**

```gitignore
# خروجی‌های بیلد و IDE
bin/
obj/
.vs/
.vscode/
packages/

# نتایج تست
TestResults/
*.trx

# Migrationهای تولیدشده EF Core
**/Migrations/*Designer.cs
**/Migrations/*ModelSnapshot.cs

# پلتفرم‌های MAUI
**/Platforms/

# فایل‌های نمونه
**/Class1.cs
**/UnitTest1.cs

# خروجی‌های Nestify
scan_output.*
docs/scan_output.*
```

## مشارکت

اگر ایده، باگ یا قالب جدیدی دارید:

- Issue باز کنید
- Pull Request بفرستید
- قالب‌های جدید را به پوشه `templates/` اضافه کنید

## لایسنس

این پروژه تحت لایسنس **MIT** منتشر شده است. آزادانه استفاده، تغییر و توزیع کنید.

---

**ساخته‌شده با ❤️ توسط [badboy1981](https://github.com/badboy1981)**

> Nestify — چون ساختار پروژه‌تان شایسته زیبایی، نظم و وضوح است. 🚀
```